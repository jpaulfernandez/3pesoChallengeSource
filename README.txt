MSP.WEB.UI

To get started,

1. Open the folder in console
2. Run an NPM install
3. After installing the dependencies, execute a gulp build command
4. Execute gulp command to see choices
4. Open browser and type in localhost address as seen in terminal:

	[Browsersync] Access URLs:
	 ---------------------------------------
	       Local: http://localhost:3000
	    External: http://10.193.157.159:3000
	 ---------------------------------------
	          UI: http://localhost:3001
	 UI External: http://10.193.157.159:3001